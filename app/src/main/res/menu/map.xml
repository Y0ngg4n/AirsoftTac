<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group
        android:id="@+id/teamSettings"
        android:checkableBehavior="single">
        <item
            android:id="@+id/settings_show_all_player"
            android:orderInCategory="100"
            android:title="Show all Player"
            app:showAsAction="never" />
        <item
            android:id="@+id/settings_show_own_team_only"
            android:orderInCategory="100"
            android:title="Show only own Team"
            app:showAsAction="never" />
        <item
            android:id="@+id/settings_show_not_assigned"
            android:orderInCategory="100"
            android:title="Show only unassigned"
            app:showAsAction="never" />
    </group>
</menu>